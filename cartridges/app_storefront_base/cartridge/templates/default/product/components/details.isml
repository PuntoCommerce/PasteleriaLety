<isif condition="${product.longDescription}">
    <div class="col-sm-12 col-md-8 col-lg-9 value content"  style="padding-left: 2.5rem;" id="collapsible-details-${loopState && loopState.count ? loopState.count : '1'}">
        <isprint value="${product.longDescription}" encoding="off" />
    </div>
</isif>
